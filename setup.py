from setuptools import find_packages, setup

__version__ = "0.2"

setup(
    name="src",
    packages=find_packages(),
    version=__version__,
    description="notebooks from the colaboratory",
    author="martin povolny",
    license="",
    install_requires=[],
)
